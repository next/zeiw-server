import { BaseGame } from '../base';

// TODO make pong use the BaseGame class.

export class PongGame extends BaseGame {
    /**
     * Create a game of pong
     * @param {UUID} id the UUID for the game
     * @param {Socket} socket the socket for the game
     */
    constructor(id, socket) {

    }
}